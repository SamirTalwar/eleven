processes:
  http:
    directory: ../processes/http
    socket: false
    config:
      port: 8080
      process: hello-world

  hello-world:
    directory: ../processes/constant
    config:
      json:
        status: 200
        body: Hello, world!
